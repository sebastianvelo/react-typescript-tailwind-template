import { FunctionComponent } from "react";

interface FormProps {
    
}
 
const Form: FunctionComponent<FormProps> = (props: FormProps) => {
    return ( 
        <form>
            
        </form>
     );
}
 
export default Form;